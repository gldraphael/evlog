version: '3.4'

services:

  evlog.web:
    image: gldraphael/evlog
    build:
      context: .
      dockerfile: src/Evlog.Web/Dockerfile
    depends_on:
      - db

  db:
    image: mysql:8.0.16
    restart: unless-stopped
