sudo: required
language: csharp
mono: none

services:
  - docker

script:
  - dotnet tool install --global Cake.Tool
  - dotnet cake --target=travis

deploy:
  provider: script
  script: ./scripts/deploy.sh
  on:
    branch: master
