sudo: required

services:
  - docker

before_install:
  - docker build -t gldraphael/evlog -f ./src/Evlog.Web/Dockerfile .

deploy:
  provider: script
  script: docker push gldraphael/evlog
  on:
    branch: master
