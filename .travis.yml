sudo: required
language: csharp
dist: xenial
mono: none
dotnet: 3.0.100

services:
  - docker

script:
  - dotnet tool restore
  - dotnet cake --target=travis

deploy:
  provider: script
  script: ./scripts/deploy.sh
  on:
    branch: master
